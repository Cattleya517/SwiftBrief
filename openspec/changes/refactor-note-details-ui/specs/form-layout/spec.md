<artifact id="specs-form-layout" change="refactor-note-details-ui" schema="spec-driven">

# Spec: Form Layout

## ADDED Requirements

### Requirement: Form Layout
#### Scenario: Section Order
- **WHEN** the `PetitionForm` component renders.
- **THEN** the `NotesSection` (Promissory Note Details) appears **immediately after** `PartySection` (Respondent).
- **AND** the `ClaimSection` (Claim Target) appears **immediately after** `NotesSection`.

</artifact>
